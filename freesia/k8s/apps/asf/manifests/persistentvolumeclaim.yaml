apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: archisteamfarm-config

spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs-client

