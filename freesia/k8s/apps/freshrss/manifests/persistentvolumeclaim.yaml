apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: freshrss-config
  labels:
    app.kubernetes.io/name: freshrss
    app: freshrss

spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
