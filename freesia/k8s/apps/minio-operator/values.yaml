console:
  ingress:
    enabled: true
    ingressClassName: "nginx"
    tls:
    - hosts:
      - console.minio.harvestasya.org
      secretName: minio-console-tls
    host: console.minio.harvestasya.org
    path: /
    pathType: Prefix
