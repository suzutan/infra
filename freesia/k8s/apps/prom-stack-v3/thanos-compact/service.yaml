apiVersion: v1
kind: Service
metadata:
  name: thanos-compact
  labels:
    app.kubernetes.io/instance: thanos
    app.kubernetes.io/component: compact

spec:
  ports:
  - port: 10902
    protocol: TCP
    targetPort: http
    name: http
  selector:
    app.kubernetes.io/name: thanos
    app.kubernetes.io/instance: thanos
    app.kubernetes.io/component: compact
