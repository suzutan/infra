apiVersion: v1
kind: Service
metadata:
  name: thanos-rule-grpc
  labels:
    app.kubernetes.io/name: thanos-rule
spec:
  type: ClusterIP
  clusterIP: None
  ports:
    - port: 10901
      protocol: TCP
      targetPort: grpc
      name: grpc
  selector:
    app.kubernetes.io/name: thanos-rule
