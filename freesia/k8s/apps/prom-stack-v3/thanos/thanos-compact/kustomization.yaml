apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- deployment.yaml
- service.yaml
- pvc-compact-data-volume.yaml

labels:
- pairs:
    app.kubernetes.io/component: compact
  includeSelectors: true
