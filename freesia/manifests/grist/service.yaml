apiVersion: v1
kind: Service
metadata:
  name: grist
  labels:
    app.kubernetes.io/name: grist
    app.kubernetes.io/instance: grist
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app.kubernetes.io/name: grist
    app.kubernetes.io/instance: grist