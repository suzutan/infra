apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: minio-operator

spec:
  routes:
    - kind: Rule
      match: Host(`minio-op-console.harvestasya.org`)
      services:
        - name: console
          port: http
