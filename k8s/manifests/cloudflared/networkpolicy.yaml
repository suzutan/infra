# cloudflared は Cloudflare API への outbound 通信のみ
# 他の Pod から cloudflared への ingress は不要
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-ingress
spec:
  podSelector: {}
  policyTypes:
  - Ingress
