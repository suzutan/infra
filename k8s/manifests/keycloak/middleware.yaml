apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: redirect-to-account
spec:
  redirectRegex:
    regex: ^https://accounts\.harvestasya\.org/?$
    replacement: https://accounts.harvestasya.org/realms/harvestasya/account/
    permanent: false
