apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: step-certificates-config
  namespace: step-ca
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-connect
  target:
    name: step-certificates-config
    creationPolicy: Owner
  dataFrom:
  - extract:
      key: step-ca/step-certificates-config
