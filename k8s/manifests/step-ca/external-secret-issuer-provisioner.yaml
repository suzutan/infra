apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: step-issuer-provisioner
  namespace: step-ca
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-connect
  target:
    name: step-issuer-provisioner
    creationPolicy: Owner
  dataFrom:
  - extract:
      key: step-ca/step-issuer-provisioner
