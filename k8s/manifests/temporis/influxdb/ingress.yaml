apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: influxdb2
  annotations:
    # RBAC: /influxdb/admin or /influxdb/viewer (using claim/groups for OIDC)
    ingress.pomerium.io/policy: |
      - allow:
          or:
            - claim/groups: /influxdb/admin
            - claim/groups: /influxdb/viewer
    ingress.pomerium.io/pass_identity_headers: "true"
    ingress.pomerium.io/set_response_headers: |
      Strict-Transport-Security: max-age=31536000; includeSubDomains; preload
      X-Frame-Options: SAMEORIGIN
      X-Content-Type-Options: nosniff
      X-XSS-Protection: 1; mode=block
      Referrer-Policy: same-origin
spec:
  ingressClassName: pomerium
  rules:
  - host: influxdb2.harvestasya.org
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: influxdb2-influxdb2
            port:
              number: 80
