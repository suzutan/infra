apiVersion: v1
kind: Service
metadata:
  name: traefik-api
  labels:
    app.kubernetes.io/name: traefik
spec:
  selector:
    app.kubernetes.io/name: traefik
    app.kubernetes.io/instance: traefik-traefik
  ports:
  - name: traefik
    port: 9000
    targetPort: 9000
    protocol: TCP
